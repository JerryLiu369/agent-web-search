# Agent Web Search — 代码评审与修复任务单

> **文档性质：** 一次性评审产物，供执行 agent 使用。修复全部合并后可删除本文件。
> **评审时间：** 2026-08-27
> **评审基线：** `origin/main` = `be13c2c`（"feat: add CLI and Skill agent mode"），版本 v0.4.0
> **仓库：** https://github.com/JerryLiu369/agent-web-search

---

## 0. 执行前必读

### 0.1 项目不变量（来自 `AGENTS.md` / `ARCHITECTURE.md`）

**动手前先读 `ARCHITECTURE.md`。** 它是本项目的设计唯一来源，`AGENTS.md` 列出了不可协商的不变量。与本任务单相关的几条：

- 只保留一个 `SearchEngine` 和一份共享的 MCP server 定义。
- **stdio 与 HTTP 的 tool schema、结果形态、错误语义必须完全一致。** 因此凡是修改 tool 行为的改动，都必须落在共享层（`agent_web_search/mcp.py` 的 `create_mcp_server`），不能只改 `mcp_http.py`。
- 运行期配置只走环境变量，不引入应用配置文件。
- provider 凭据留在服务端，不作为 MCP tool 参数。
- 若某项改动触及上述不变量，**必须在同一个改动里更新 `ARCHITECTURE.md`**（尤其是 "Compatibility and testing" 那份清单）。
- `pytest` 与 `ruff` 必须在同一个改动里保持绿色。

### 0.2 环境与命令

仓库内已有 `.venv`（Python 3.11，ruff 0.16.4）。

```bash
# 测试
.venv/Scripts/python.exe -m pytest -q          # Windows
python -m pytest -q                            # 已激活环境

# lint
.venv/Scripts/python.exe -m ruff check .

# 从零搭建
uv venv && uv pip install -e '.[dev]'
```

本机 git 访问 GitHub 需要走本地代理：

```powershell
$env:HTTPS_PROXY='http://127.0.0.1:7890'; $env:HTTP_PROXY='http://127.0.0.1:7890'
```

### 0.3 基线状态

| 检查项 | 结果 |
| --- | --- |
| `ruff check .` | 全绿 |
| `pytest -q` | **56 passed / 1 failed** |
| 唯一失败项 | `tests/test_exa_parse.py::test_exa_mcp_path_used_without_key` |

该失败是真实缺陷（见 F1），**不是环境噪音，也不要通过跳过测试来"修复"**。CI 之所以是绿的，只因为 CI runner 上没有任何 provider key。

### 0.4 关于本任务单的可信度

- **P0 / P1 段落里的每一条都已在本机实测复现**，含具体命令与实际输出。
- **P2 段落是设计层建议**，未必都要做，需要 owner 判断；标注了各自的理由。
- 结论中已明确排除的一项：鉴权中间件的路径匹配**目前不存在绕过**（见 F9，附实测矩阵）。不要把它当成安全漏洞来修。

### 0.5 整体评价（供执行者建立判断基准）

架构是本项目最强的部分，不需要重构。`ARCHITECTURE.md` + `AGENTS.md` 把不变量写成了可执行契约，代码确实守住了：一个 `SearchEngine`、一份 tool schema、一个 `search_result_payload()` 供 CLI/MCP/Hermes 共用，10 个 provider 全部收敛到同一个 `ProviderResponse`。provider 失败隔离、`to_dict()` 只暴露 `answer`+`results`、中英文 README 章节 1:1 对齐——这些都做得比同类项目干净。

**问题集中在两处：输入校验、启动期健壮性。** 修复应当是局部的、加固性质的，不要顺手改架构。

---

## 1. 任务清单总览

| ID | 优先级 | 主题 | 主要文件 |
| --- | :---: | --- | --- |
| F1 | P0 | 测试套件不隔离环境变量，本机必挂 | `tests/conftest.py`（新增） |
| F2 | P0 | 被禁用 provider 的配置错误拖垮整个进程启动 | `registry.py`, `engine.py` |
| F3 | P0 | `AGENT_WEB_SEARCH_TIMEOUT` 非法值裸崩 | `engine.py` |
| F4 | P0 | MCP 完全不校验自己声明的 inputSchema | `mcp.py` |
| F5 | P0 | 空 query 经 MCP 漏出未捕获异常 | `mcp.py` |
| F6 | P1 | 12 个 `# noqa: BLE001` 全部失效 | `pyproject.toml` + 各 provider |
| F7 | P1 | `grok.py` 的 `searched` 判断方向写反 | `providers/grok.py` |
| F8 | P1 | timeout 语义与文档不符（最坏 3×） | 文档 / `parallel.py` |
| F9 | — | 鉴权路径匹配（**无漏洞**，建议结构性加固） | `mcp_http.py` |
| F10 | P1 | 匿名模式无绑定约束 | `mcp_http.py` |
| F11 | P2 | 响应内 token 浪费 | `models.py` |
| F12 | P2 | stdio 用户被迫安装 web server | `pyproject.toml` |
| F13–F19 | P2 | 零散清理项 | 见 §5 |
| F20 | P1 | `from_mapping` 对字符串 `providers` 按字符迭代（Python API 脚枪） | `models.py` |

**建议提交分组**（每组独立可回滚，各自保持 pytest + ruff 绿色）：

1. `test: isolate provider environment variables`（F1）
2. `fix: validate web_search arguments in the shared MCP server`（F4 + F5 + F20，附 `ARCHITECTURE.md` 更新）
3. `fix: tolerate provider misconfiguration at startup`（F2 + F3 + F13）
4. `chore: enforce the lint rules the code already assumes`（F6 + F7 + F17）
5. `docs: correct timeout and Parallel result-cap semantics`（F8 + F18）
6. 其余 P2 按 owner 决定单独处理

**推荐执行顺序：** 1 → 2 → 3 → 4 → 5。理由：组 1 必须最先落地——组 2/3 的新测试全部依赖环境隔离，否则在配了 key 的开发机上必红；组 3 会重构 `SearchEngine` 构造路径，先让组 2 的行为测试锁住现状再做重构，回归风险最小；组 4/5 与行为无关，任何时候做都行。

---

## 1.5 执行进度跟踪（2026-08-27 大修）

> 本节是整个大修的唯一进度面板。每完成一组，把对应复选框打勾并**填写实测结果**（不是"应该绿"，是跑出来的数字）。全部打勾后本文件可删。

- [x] **组 1** `test: isolate provider environment variables`（F1）— 新增 `tests/conftest.py` autouse fixture，前缀匹配清空全部 provider 凭据与 `AGENT_WEB_SEARCH_*` 变量；删除 `test_parallel.py` 两处冗余 `delenv`
  - 裸环境 `pytest -q`：**57 passed**（1.16s，本机 .venv Python 3.11）
  - 污染环境（设 `EXA_API_KEY`/`BRAVE_SEARCH_API_KEY`/`PERPLEXITY_API_KEY`/`YDC_API_KEY` 等 9 个凭据 + `AGENT_WEB_SEARCH_TIMEOUT`）`pytest -q`：**57 passed**（1.14s）
  - ruff：**All checks passed**
- [x] **组 2** `fix: validate web_search arguments in the shared MCP server`（F4 + F5 + F20，附 `ARCHITECTURE.md` 更新）
  - stdio + HTTP 双路径非法参数测试：**通过**（新增 16 项相关测试）
  - 裸环境 / 污染环境 `pytest -q`：**88 passed**（1.22s；组 2 完成时裸环境）
  - ruff：**All checks passed**
- [x] **组 3** `fix: tolerate provider misconfiguration at startup`（F2 + F3 + F13）
  - 禁用 provider 配置错不影响启动测试：**通过**（`ark` 的空模型配置不影响 ddgs/exa/parallel 启动）
  - timeout 非法值测试：**通过**（非数值、0、负数、inf、nan 均返回可读 `ValueError`）
  - 裸环境 / 污染环境 `pytest -q`：**96 passed**（1.20s；组 3 完成时裸环境）
  - ruff：**All checks passed**
- [x] **组 4** `chore: enforce the lint rules the code already assumes`（F6 + F7 + F17）
  - 收紧后 ruff 统计（应为 0）：**0**（`pyproject.toml` 启用 `E4/E7/E9/F + BLE + I/UP/B/SIM/RUF/C4/PT/E501`，27 处告警全修：13× E501 行长、2× SIM105 → `contextlib.suppress`、3× pytest.raises 重构（PT011/15/17 + B011）、2× PT018 拆分、1× RUF001 中文标点 noqa；12 个 `# noqa: BLE001` 恢复生效，RUF100 unused-noqa 归零；F7 方向修复 + F17 死代码清理与 `_RawResponse` 上移）
  - grok `mode=both` 新测试：**通过**（`test_grok_parse_both_mode_matches_either_tool`，`parse(..., ["web_search","x_search"])` 匹配 `web_search_call`）
  - `pytest -q`：**97 passed**（1.22s，裸环境与污染环境均通过）
- [x] **组 5** `docs: correct timeout and Parallel result-cap semantics`（F8 + F18）
  - 中英 README 同步改动：**完成**（`AGENT_WEB_SEARCH_TIMEOUT` 描述改为"单次上游 HTTP 调用的 socket 超时"，注明 keyless Parallel 最坏 3× / ARK 续写最坏 2×；中英故障排查均新增"MCP 客户端 tool 超时应大于 3× 该值"提示；Parallel `max_results` 表格行区分 REST 原生 / keyless MCP 客户端截断）
- [ ] **组 6** P2 按需（F11/F12/F13–F19 中 owner 挑选的项，做完在此登记）
  - 已做项：**F11**（`to_dict` 空 `answer` 省略键、result 行去掉冗余 `provider` 字段；README 中英、SKILL.md 示例、`ARCHITECTURE.md` 契约与 `test_core.py` 断言同步更新）；**F12**（`pyproject.toml` 删除 `starlette>=0.37`/`uvicorn>=0.30` 硬依赖——实测 `mcp` metadata 无条件 require 两者，stdio/pipx 安装不再携带 HTTP server 约束；留注释防止误补回）
  - `pytest -q` / ruff：**97 passed**（1.18s，F12 后复跑）/ **All checks passed**；`mcp_http` import 冒烟通过

**里程碑定义：** 每组一个独立 commit，push 到 unstable，各自保持 pytest + ruff 绿色后才算完成。

---

## 2. P0 — 必修

### F1. 测试套件不隔离环境变量，任何配了 key 的开发机都会红

**位置：** `tests/test_exa_parse.py:114`，根因在 `agent_web_search/providers/exa.py:30`

**复现：**

```bash
# 前置：环境里存在 EXA_API_KEY
.venv/Scripts/python.exe -m pytest -q tests/test_exa_parse.py
```

实际输出：

```
E  AssertionError: assert 'https://api.exa.ai/search' == 'https://mcp.exa.ai/mcp'
tests/test_exa_parse.py:117: AssertionError
1 failed
```

**根因：** 测试用 `ExaProvider(api_key="")` 表达"无 key"，但 provider 构造函数是

```python
self.api_key = api_key or os.getenv("EXA_API_KEY", "")
```

显式传入的 `""` 是 falsy，会被当作"未提供"从而回落到环境变量。于是无 key 分支根本没被测到，反而走了付费 API 分支。

**同类隐患（同样断言 "XXX_API_KEY is not set"，配了对应 key 就会红）：**

- `tests/test_brave.py:24` — `BRAVE_SEARCH_API_KEY`
- `tests/test_perplexity.py:23` — `PERPLEXITY_API_KEY`
- `tests/test_you.py:23` — `YDC_API_KEY`

`tests/test_parallel.py:32` 和 `:102` 已经用 `monkeypatch.delenv("PARALLEL_API_KEY")` 打了补丁——说明这个坑之前被踩到过，但只修了一处，没有系统化。

**修复方案：** 新增 `tests/conftest.py`，用 autouse fixture 清空全部相关环境变量。这一个文件同时消除上述所有隐患，且让测试结果不再依赖开发机状态。

需要清理的变量：所有 provider 凭据（`ARK_API_KEY`、`BRAVE_SEARCH_API_KEY`、`EXA_API_KEY`、`EXA_MCP_URL`、`GEMINI_API_KEY`、`XAI_API_KEY`、`PARALLEL_API_KEY`、`PERPLEXITY_API_KEY`、`TAVILY_API_KEY`、`YDC_API_KEY`）+ 全部 `AGENT_WEB_SEARCH_*`。建议用「遍历 `os.environ` 前缀匹配」而非硬编码列表，避免以后新增 provider 时再次漏掉。

清完之后，`test_parallel.py` 里那两处 `delenv` 就成了冗余，可一并删除。

**可选的更深修复（需 owner 同意）：** 把 provider 构造签名从 `api_key: str | None = None` + `api_key or os.getenv(...)` 改成 `api_key if api_key is not None else os.getenv(...)`，让显式 `""` 真正表示"无 key"。这会影响全部 10 个 provider 的语义，**属于公共 Python API 变更**，不要顺手做；先用 conftest 修好测试，再单独提案。

**验证：** 在设置了 `EXA_API_KEY` / `BRAVE_SEARCH_API_KEY` / `PERPLEXITY_API_KEY` / `YDC_API_KEY` 的环境下 `pytest -q` 全绿；在裸环境下同样全绿。

---

### F2. 被禁用 provider 的配置错误会拖垮整个进程启动

**位置：** `agent_web_search/registry.py:75`（`create_provider_pool`）、`agent_web_search/engine.py:17-19`、`agent_web_search/model_pool.py:23`

**复现：**

```bash
AGENT_WEB_SEARCH_PROVIDERS=ddgs,exa,parallel \
AGENT_WEB_SEARCH_ARK_MODELS=, \
python -c "from agent_web_search import SearchEngine; print(SearchEngine().enabled_provider_names)"
```

实际输出：

```
File ".../agent_web_search/providers/ark.py", line 33, in __init__
    self.models = configured_models(
File ".../agent_web_search/model_pool.py", line 23, in configured_models
    raise ValueError(f"{env_name} must contain at least one model")
ValueError: AGENT_WEB_SEARCH_ARK_MODELS must contain at least one model
```

**问题：** `ark` 根本不在启用列表里，MCP server 却完全起不来，而且抛的是裸 traceback。原因是 `create_provider_pool()` 无条件实例化 `PROVIDER_SPECS` 里的全部 10 个 provider，不看 `AGENT_WEB_SEARCH_PROVIDERS`；而 `ArkProvider.__init__` → `configured_models()` 会抛 `ValueError`。

注意 `AGENT_WEB_SEARCH_ARK_MODELS=,` 这种值并不刻意刁难——`","`、`" , "`、误粘贴的换行都会触发（`raw.strip()` 后仍为真但拆出来全是空串）。

**修复方案（推荐）：** 改为按需构造，只实例化启用集合内的 provider。这需要把「读 `AGENT_WEB_SEARCH_PROVIDERS`」的时机提到构造 provider 之前。

注意 `engine.py` 当前保留了 `self._all_providers`（全量）和 `self.providers`（启用子集）两份引用，而 `search()` 在 `engine.py:54` 用的是 `self._all_providers[name]`。由于 `self.providers` 恒为 `self._all_providers` 的子集，两者等价（见 F13），改成按需构造时可以顺势把 `_all_providers` 一并去掉，简化为单一 dict。

**重构时必须保住的语义（易漏）：** `providers is not None` 分支表示"显式注入 provider 实例"，此时**忽略 `AGENT_WEB_SEARCH_PROVIDERS` 环境变量**、`self.providers = all_providers` 原样使用。现有多个测试（如注入 fake provider 的用例）依赖这一语义，按需构造的重构不得改变它——即：显式注入时全量接收，环境驱动时按需构造。

**收益：** 除修掉本缺陷，还省掉 7 个未使用 provider 的构造开销（CLI 每次调用都会付这笔成本）。

**兜底要求：** 即使改成按需构造，`configured_models` 的 `ValueError` 落到启用中的 provider 上时，也不应该是裸 traceback。启动期配置错误应当给出可读信息（参照 `mcp_http.py:32` 的 `_parse_port` 风格）。

**验证：** 新增测试——设置 `AGENT_WEB_SEARCH_ARK_MODELS=,` 且 `AGENT_WEB_SEARCH_PROVIDERS` 不含 `ark` 时，`SearchEngine()` 构造成功且 `enabled_provider_names == ["ddgs","exa","parallel"]`。

---

### F3. `AGENT_WEB_SEARCH_TIMEOUT` 非法值裸崩

**位置：** `agent_web_search/engine.py:13`

**复现：**

```bash
AGENT_WEB_SEARCH_TIMEOUT=abc python -c "from agent_web_search import SearchEngine; SearchEngine()"
```

实际输出：

```
File ".../agent_web_search/engine.py", line 13, in __init__
    float(os.getenv("AGENT_WEB_SEARCH_TIMEOUT", "60"))
ValueError: could not convert string to float: 'abc'
```

**问题：** 同一个项目里两套标准。`mcp_http.py:32` 的 `_parse_port` 有干净的错误信息（`"HTTP port must be an integer"` / `"HTTP port must be between 1 and 65535"`），而 timeout 直接裸崩。

**修复方案：** 给出与 `_parse_port` 一致风格的错误信息，并校验取值为正数。是「拒绝启动并给出清晰信息」还是「回落到默认 60 并告警」由 owner 定；建议前者，与 `_parse_port` 保持一致。

**验证：** 新增测试覆盖非数值与非正数两种输入。

---

### F4. MCP 完全不校验它自己声明的 inputSchema

**位置：** `agent_web_search/mcp.py:40-53`（`call_tool`）；schema 定义在 `agent_web_search/schema.py`

**问题：** `schema.py` 向客户端声明了 `enum`、`minLength: 1`、`maximum: 20`、`additionalProperties: false`，但 MCP lowlevel `Server` 不做 inputSchema 校验，`call_tool` 也没有自己校验，因此这些约束**全部只是文档**。

**实测矩阵**（真实 `SearchEngine`，启用集合 `["ddgs","exa","parallel"]`，经 `POST /mcp` 发起 `tools/call`）：

| 输入 arguments | 期望 | 实际 |
| --- | --- | --- |
| `{"query":"hi","providers":["nope"]}` | 拒绝（不在 enum 内） | **200** + `{"query":"hi","providers":{}}` |
| `{"query":"hi","providers":"ddgs"}` | 拒绝（类型应为 array） | **200** + `{"query":"hi","providers":{}}` |
| `{"query":"hi","max_results":999}` | 拒绝（maximum 20） | 接受，engine 收到 999 |
| `{"query":"hi","bogus_extra":1}` | 拒绝（additionalProperties false） | 静默忽略 |

**最严重的是前两行：返回的是成功形态的空文档。** 调用方 Agent 会把它读成"网上没有相关内容"，而不是"你请求了一个没启用的 provider"。

`providers:"ddgs"` 这一行值得单独说明其机制：`models.py:40` 的 `list(dict.fromkeys(str(name) for name in self.providers))` 会把字符串按字符迭代成 `['d','g','s']`，随后被 `engine.py:50` 全部过滤掉，最终得到空集且**不产生任何 error**——因为 `all_providers_failed` 的定义是 `not providers and bool(failed_provider_errors)`，而此处 `failed_provider_errors` 为空。

**语义分叉：** 同样的输入，CLI 会明确拒绝（`cli.py:39-48`，exit 2，`"providers are not enabled: ..."`）。README 与 `ARCHITECTURE.md` 都宣称 CLI 与 MCP 共用同一份契约，此处实际分叉了。

**修复方案：**

1. 在 `create_mcp_server` 的 `call_tool` 里校验 `params.arguments`，失败时返回**结构化 tool error**（`isError=True` + 与 `all_providers_failed` 同风格的 payload，建议新错误码 `invalid_arguments`，含具体字段诊断）。
2. 另外补一条语义校验：当 `providers` 解析后的有效集合为空时，同样返回 tool error 而非空成功文档——与 CLI 行为对齐。
3. 复用 `agent_web_search/output.py` 的模式承载新错误码，保持"一份 payload 构造逻辑"这条不变量。

**校验实现选型（owner 已倾向手写）：** schema 只有 6 个字段，手写针对这 6 个字段的轻量校验，不引入 jsonschema——与项目"最小依赖"取向一致。jsonschema 虽是 `mcp` 的传递依赖，但显式声明它进 `dependencies` 与 F12（删依赖）的方向相悖。

**位置要求（不变量）：** 修复必须落在 `mcp.py` 的共享 server factory 内，使 stdio 与 HTTP 自动一致。**不要**改 `mcp_http.py`。

**校验时机（关键细节）：** 校验必须发生在 `SearchRequest.from_mapping(args)` **之前**。`from_mapping` 对字符串型 `providers` 会按字符迭代（见 F20），先经过它再校验就晚了。

**必须同步更新 `ARCHITECTURE.md`：** 在 "Compatibility and testing" 清单里增加一条——非法 `web_search` 参数在两种 transport 上都返回结构化 tool error。

**验证：** 上表 4 行输入各写一个测试；stdio 与 HTTP 两条路径都要覆盖（`tests/test_mcp.py` + `tests/test_mcp_http.py`）。

---

### F5. 空 query 经 MCP 漏出未捕获异常

**位置：** `agent_web_search/mcp.py:48`，异常源自 `agent_web_search/engine.py:46`

**复现：** 经 `POST /mcp` 发起 `tools/call`，arguments 为 `{"query":""}`、`{"query":"   "}` 或 `{}`。

实际响应：

```json
{"jsonrpc": "2.0", "id": 7, "error": {"code": 0, "message": "query must not be empty"}}
```

同时服务端日志打出完整 traceback（`handler for 'tools/call' raised` + 两段堆栈）。

**三个问题：**

1. `engine.py:46` 的 `raise ValueError("query must not be empty")` 在 `mcp.py:48` 没有被捕获，直接漏进 MCP SDK。
2. 结果是 **JSON-RPC 协议层错误**而非 tool error，`code: 0` 不是有意义的 JSON-RPC 错误码（无效参数应为 `-32602`），且完全绕过了项目自己那套结构化错误契约。
3. 每次都刷一段 traceback 到日志，正常的用户输入错误不该产生这种噪音。

对比：CLI 这一侧是正确的（`cli.py:60` 捕获 `ValueError` → `p.error()` → exit 2）。

**补充说明（无需修）：** 已确认该 traceback 走 Python logging 默认落到 **stderr**，代码里没有任何 logging 配置会把它导向 stdout，因此不违反 `ARCHITECTURE.md` 中"stdio 模式绝不向 stdout 写日志"这条不变量。改动时请勿引入向 stdout 输出的 logging 配置。

**修复方案：** 与 F4 合并处理——空 query 属于参数非法的一种，应走同一条结构化 tool error 路径。修完后 `code: 0` 这条协议层错误应当消失。

**验证：** 三种空 query 输入在 stdio 与 HTTP 上都返回 `isError=True` 的结构化 payload，且日志无 traceback。

---

## 3. P1 — 应修

### F6. 12 个 `# noqa: BLE001` 全部失效

**位置：** `pyproject.toml:54-58`；noqa 分散在各 provider 与 `engine.py:61`

**问题：** ruff 默认 select 是 `["E4", "E7", "E9", "F"]`，`BLE`（flake8-blind-except）不在其中。因此代码里所有 `# noqa: BLE001` 都是纯装饰——它们压制的规则从未启用。

**实测：**

```bash
.venv/Scripts/python.exe -m ruff check --select RUF100 agent_web_search tests
# → 12 × RUF100 unused-noqa
```

代码读起来像是在遵守一套严格 lint，配置却没有启用它。这类"看似受约束、实则无约束"的状态比不加 lint 更容易误导后续维护者。

**更严格配置下的完整代价（已实测）：**

```bash
.venv/Scripts/python.exe -m ruff check --select I,UP,B,SIM,RUF,C4,PT,E501 --statistics agent_web_search tests
```

```
12  E501    line-too-long
12  RUF100  unused-noqa            [*] 可自动修
 2  PT018   pytest-composite-assertion
 2  SIM105  suppressible-exception
 1  B011    assert-false
 1  PT015   pytest-assert-always-false
 1  PT017   pytest-assert-in-except
 1  RUF001  ambiguous-unicode-character-string
——— 合计 32 条
```

总量很小。本项目的 import 排序已天然符合 `I`，零成本。

**修复方案：** 在 `pyproject.toml` 的 `[tool.ruff.lint]` 里显式 select，建议 `["E", "F", "I", "UP", "B", "SIM", "RUF"]`，并二选一：

- 要么把 `BLE` 也加进去，让那 12 个 noqa 名副其实（provider 必须隔离异常，这些 `except Exception` 是有意为之，noqa 是正确表达）；
- 要么删掉全部 12 个 noqa。

推荐前者——它准确记录了设计意图。

`E501` 那 12 条注意：`providers/parallel.py:248` 是 90 字符，`exa.py:196`、`grok.py:137`、`gemini.py:112`、`brave.py:37` 等 f-string 错误信息也超长，需要折行。

**验证：** `ruff check .` 在新配置下全绿。

---

### F7. `providers/grok.py` 的 `searched` 判断方向写反

**位置：** `agent_web_search/providers/grok.py:43-46`，调用点在 `:129`

```python
searched |= (
    item_type in {"web_search_call", "x_search_call"}
    or tool_name in item_type          # ← 方向反了
)
```

调用方传入的是 `",".join(tool_names)`（`grok.py:129`），在 `mode == "both"` 时其值为 `"web_search,x_search"`。拿这一整个串去做 `in item_type` 判断，**永远不可能为真**。

**实际影响≈0**：`searched` 不进公开响应（`models.py:83` 的 `to_dict()` 只输出 `answer` 和 `results`），且前半个集合成员判断已覆盖常规情况。但逻辑是错的，会误导读者。

**修复方案：** 改为 `any(name in item_type for name in tool_names)`，并把参数类型从 `str` 改成 `list[str]`（或保持 `str` 但由调用方只传单个名字）。既然影响为 0，选最易读的写法即可。

**验证：** `tests/test_optional_providers.py:80` 已覆盖单 tool 场景，补一个 `mode="both"` 的场景。

---

### F8. timeout 语义与文档不符（最坏 3×）

**位置：** 文档 `README.md:354` / `README.zh-CN.md` 对应行；行为在 `providers/parallel.py:190-237`、`providers/ark.py:134`、`engine.py:52-64`

**问题：** README 把 `AGENT_WEB_SEARCH_TIMEOUT` 描述为 "Per-provider timeout in seconds"，实际它是**单次 HTTP 调用**的 socket timeout：

- keyless `parallel` 串行发 3 个请求（`initialize` → `notifications/initialized` → `tools/call`），最坏 **3×** 声明值；
- `ark` 在 `answer` 短于 100 字符时追加 `_continue()` 请求，最坏 **2×**；
- `engine.py:57` 的 `as_completed(futures)` 没有总 deadline，`ThreadPoolExecutor.__exit__` 又是 `shutdown(wait=True)`，因此整次搜索的上限由最慢的那个 provider 链决定。

对有自身 tool timeout 的 MCP 客户端来说，这个差异会真实咬人：配了 60s 却可能等 180s。

**修复方案（二选一，倾向 A）：**

- **A（仅改文档）：** 把 README 两个语言版本的措辞改为准确描述——单次上游 HTTP 调用的超时，并说明多步 provider（`parallel` keyless / `ark` 续写）的累计上界。成本最低，无行为变更。
- **B（改行为）：** 引入 per-provider 墙钟预算，多步 provider 在各步之间递减剩余预算。更正确但改动面大，需 owner 决策。

若选 A，请同时在 README 的 troubleshooting 段落加一句提示：MCP 客户端的 tool timeout 应大于 `3 × AGENT_WEB_SEARCH_TIMEOUT`。

---

### F9. 鉴权中间件路径匹配 — **目前无漏洞**，建议结构性加固

**位置：** `agent_web_search/mcp_http.py:95`

**背景：** `MCPHTTPPolicyMiddleware` 仅在 `scope["path"].rstrip("/") == "/mcp"` 时执行鉴权，其余路径直接放行。这看起来像典型的鉴权绕过风险，因此做了实测。

**实测结论：不存在绕过。** 完整矩阵（无 token 发送 `tools/call`）：

| 路径 | 无 token | 带 token | 是否触达 engine |
| --- | --- | --- | :---: |
| `/mcp` | 401 | 200 | 仅带 token 时 |
| `/mcp/` | 401 | 200 | 仅带 token 时 |
| `/mcp//` | 401 | 200 | 仅带 token 时 |
| `/mcp/../mcp` | 401 | 200 | 仅带 token 时 |
| `/mcp/foo` | 404 | 404 | 否 |
| `//mcp` | 404 | 404 | 否 |
| `/Mcp` | 404 | 404 | 否 |

凡是能触达 transport 的路径都被 guard 覆盖（`rstrip("/")` 正确处理了尾斜杠变体），其余在路由层就 404 了。

**因此：不要把它当漏洞修，也不要为此发安全公告。**

**但值得加固的理由：** guard 与路由是**各自独立声明**的（guard 在 `mcp_http.py:95` 硬编码 `"/mcp"`，路由在 `:189` 由 `streamable_http_path="/mcp"` 决定）。一旦 `streamable_http_path` 变化、或 SDK 未来让 transport 长出子路径，鉴权会**静默失效**且没有任何测试会报警。

**建议方案：** 改成 deny-by-default——除 `/healthz` 白名单外全部要求鉴权。这样安全性来自结构而非巧合。同时新增一个测试：断言"除 `/healthz` 外任意路径在无 token 时都不返回 2xx"。

优先级定 P1 是因为加固成本极低、且能防住一类未来回归；**不是**因为当前有风险。

---

### F10. 匿名模式无绑定约束

**位置：** `agent_web_search/mcp_http.py:153-159`（`_auth_token`）、`:170`（`_transport_security`）、`:58`（默认 host `0.0.0.0`）

**问题：**

- DNS rebinding 保护只在设置了 `AGENT_WEB_SEARCH_HTTP_ALLOWED_HOSTS` 时才开启（`:170` 未设则 `return None`），默认关闭。
- `AGENT_WEB_SEARCH_ALLOW_ANONYMOUS=true` 不对绑定地址做任何要求，而默认 host 是 `0.0.0.0`。

两者叠加：在容器里设 `ALLOW_ANONYMOUS=true` 就是一个公网开放的搜索代理，会消耗部署者的 provider 配额。

**当前缓解（做得对，无需改）：** 默认值是 `false`；`.env.example:10` 与 `template.yaml:89-90` 都显式写成 `false`；README 与 `ARCHITECTURE.md` 都把它描述为"仅用于可信网络或一次性演示"的显式 opt-out。

**建议方案：** 当 `allow_anonymous` 为真时，要求 host 为 loopback，或要求显式设置了 `ALLOWED_HOSTS`；否则拒绝启动并给出清晰原因。这与 `_auth_token()` 现有的"拒绝启动 + 说明理由"风格一致。退一步的最低方案是启动时打一条醒目 warning。

MCP 规范本身也建议本地 HTTP server 开启 Host 校验以防 DNS rebinding，可在 `ARCHITECTURE.md` 的 "HTTP authentication" 段落引用。

**验证：** 覆盖两种情形——匿名 + 非 loopback host 应被拒绝；匿名 + loopback 应可启动。

---

### F20. `from_mapping` 对字符串 `providers` 按字符迭代（Python API 脚枪）

**位置：** `agent_web_search/models.py:40`（`normalized()`），入口在 `SearchRequest.from_mapping`

**问题：** F4 修的是 MCP 入口，但直接构造 `SearchRequest(providers="ddgs")` 的 Python API 用户会踩同一个坑：

```python
providers = list(dict.fromkeys(str(name) for name in self.providers))
```

对字符串 `"ddgs"` 会迭代成 `['d', 'g', 's']`，随后在 `engine.search()` 里被全部过滤掉，得到空 provider 集。与 F4 第 2 点的组合结果：**不产生任何 error，静默返回空结果**（`all_providers_failed` 的定义是 `not providers and bool(failed_provider_errors)`，此处 `failed_provider_errors` 为空）。

`str(name) for name in self.providers` 这个看似防御性的写法实为根源——它掩盖了类型错误而非暴露它。

**修复方案：** 在 `normalized()`（或 `from_mapping`）里拒绝非 list/tuple 类型的 `providers`，抛 `ValueError`，信息含实际收到的类型。一行守卫，让 Python API 与 MCP/CLI 的错误语义对齐。

**验证：** 新增测试——`SearchRequest(providers="ddgs")` 抛 `ValueError`；合法的 `["ddgs"]` 照常工作。

---

## 4. P2 — 建议（需 owner 判断）

### F11. 响应内 token 浪费

**位置：** `agent_web_search/models.py:83-106`（`to_dict`）

这份 payload 直接进 LLM context，因此冗余字段有真实成本：

- `:101` 对每个 provider 无条件输出 `"answer": ""`——关键词类 provider（ddgs / brave / exa / parallel / perplexity / tavily / you）永远是空串。
- `:89` 每一行 result 都带 `"provider": "<name>"`，而它已经是外层 dict 的 key。3 个 provider × 10 条结果 = 30 个完全冗余的字段。

**建议：** `answer` 为空时省略该键（与已有的 `published_at` / `author` 条件输出保持一致）；`provider` 逐行重复可考虑去掉。

**注意：** 这是**公开响应格式变更**，涉及 README §Response format、`skills/agent-web-search/SKILL.md` 的示例、以及 `tests/test_core.py:100-110` 的断言。与 README 里 "Focused responses" 的自我定位一致，但需 owner 拍板是否算 breaking change。

### F12. stdio 用户被迫安装 web server

**位置：** `pyproject.toml:28-34`

`starlette>=0.37` 与 `uvicorn>=0.30` 被声明为硬依赖，但：

1. `mcp` 自身已经 require 两者（实测 metadata：`starlette>=0.27; python_version < '3.14'`、`starlette>=0.48.0; python_version >= '3.14'`、`uvicorn>=0.31.1; sys_platform != 'emscripten'`）；
2. stdio 是默认模式，也是 `pipx install` 的主要场景，这些用户永远不会运行 HTTP server；
3. 本项目声明的 `starlette>=0.37` 在 Python 3.14 上比 mcp 自己的 `>=0.48` **更弱**，属于无效约束。

**建议：** 要么删掉这两行依赖 mcp 的传递依赖，要么拆成 `[project.optional-dependencies] http = ["starlette>=0.48", "uvicorn>=0.31.1"]`。若选后者，`mcp_http.py` 需要在 import 失败时给出可读的安装提示。

### F13–F19. 零散清理项

| ID | 位置 | 问题 |
| --- | --- | --- |
| F13 | `engine.py:54` | `self._all_providers[name]` 可直接写 `self.providers[name]`（`self.providers` 恒为子集，两分支均成立）。多一层无意义间接；随 F2 一并清理 |
| F14 | `providers/parallel.py:20-22` | `PACKAGE_VERSION` 兜底值 `"0.1.0"` 已过期。`tests/test_version.py` 只守 `pyproject.toml` / `plugin.yaml` / `__init__.py` 三处，管不到这里。改用包内 `__version__` 时注意循环导入——`agent_web_search/__init__.py:7` 的 `__version__` 定义在 import 之后，需先上移 |
| F15 | `providers/ddgs.py:19` | 报错文案 `pip install 'agent_web_search[ddgs]'` 三重错误：包名应为 `agent-web-search-mcp`、不存在 `ddgs` extra、且 `ddgs` 是硬依赖所以该 `ImportError` 分支实为死代码 |
| F16 | `providers/ark.py:131` | `len(parsed.answer.strip()) < 100` 这个 magic number 会触发**第二次计费请求**。抽成命名常量并加注释说明阈值依据 |
| F17 | `tests/test_exa_parse.py:98` | 残留死代码 `return _FakeResponse({}) if False else _RawResponse(body)`。另外 `_RawResponse` 类定义在使用点之后（靠运行期解析侥幸可用），应上移 |
| F18 | `README.md:524` 及中文版对应行 | 表格称 Parallel 的 `max_results` 为 "Native"，但 keyless MCP 路径实际是客户端截断（`parallel.py:243` 的 `results[:max_results]`）。付费 REST 路径才是原生的。需区分说明 |
| F19 | `providers/exa.py:144` | `min(100, request.max_results)`，而 `models.py:43` 的 `normalized()` 早已封顶 20。死上界 |

---

## 5. 明确不在范围内

以下项已评估，**无需处理**，列出以免重复劳动：

- **鉴权绕过** — 不存在，见 F9 实测矩阵。
- **stdio 向 stdout 写日志** — 不存在。代码里没有任何 logging 配置，SDK 的异常日志走 Python logging 默认落 stderr。见 F5 补充说明。
- **provider 错误信息泄露 API key** — 检查过全部 10 个 provider 的错误构造路径。key 一律走 header，错误信息只含上游 response body（截断 500 字符）与异常类名，未发现泄露渠道。但请注意 `provider_errors` 是**上游原样输出**，公开部署时持有效 token 者可见上游报错原文——这是可接受的设计，不必改。
- **中英文 README 漂移** — 已逐节比对，28 个章节 1:1 对齐，无缺失。
- **版本一致性** — `pyproject.toml` / `plugin.yaml` / `agent_web_search/__init__.py` 三处均为 `0.4.0`，`tests/test_version.py` 有守卫。唯一未被守卫的是 F14。
- **git 工作树** — 干净，`build/` `dist/` `__pycache__` 均已被 `.gitignore` 与 `.dockerignore` 覆盖。
- **`.github/workflows`** — CI 覆盖 Python 3.10–3.13 + ruff + docker build；publish 有 tag/version 一致性校验。配置合理，无需改动。

---

## 6. 完成标准

全部 P0 + P1 合并后应满足：

1. `pytest -q` 在**裸环境**与**配了多个 provider key 的环境**下都全绿。
2. `ruff check .` 在收紧后的规则集下全绿。
3. 非法 `web_search` 参数（含空 query、未启用 provider、类型错误、超界数值）在 **stdio 与 HTTP 两条路径上**都返回结构化 tool error，且日志无 traceback。
4. 被禁用 provider 的配置错误不再影响进程启动。
5. `ARCHITECTURE.md` 的 "Compatibility and testing" 清单已按 §0.1 要求同步更新。
6. README 中英双语的 timeout 与 Parallel `max_results` 描述与代码行为一致。
7. Python API 直接构造 `SearchRequest(providers="ddgs")` 抛 `ValueError`，不再静默返回空结果（F20）。
